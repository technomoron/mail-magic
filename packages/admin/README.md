# @technomoron/mail-magic-admin

Placeholder Vue admin UI for mail-magic.

## Notes

- This package currently ships a minimal static placeholder in `dist/`.
- A full Vue-based admin interface can be added later using the `src/` + Vite setup.
