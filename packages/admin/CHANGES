CHANGES
=======

Unreleased (2026-03-07)
-----------------------

* chore(release): add package-local `release:preflight` support so a single package can run cleanbuild, tests, and local release checks from its own directory.
* (Changes generated/assisted by Codex (profile: openai-gpt-5-codex/medium).)

v0.1.0 (2026-03-07)
-------------------

* chore(release): keep the placeholder admin package on an early `0.1.0` line instead of the main `2.0.0-beta1` prerelease track.
* chore(release): add shared local/CI release verification tooling so package release checks can be exercised before tagging.
* chore(release): add a shared `npm publish --dry-run` path so package publication can be smoke-tested locally before tagging.
* (Changes generated/assisted by Codex (profile: openai-gpt-5-codex/medium).)

v0.0.4 (2026-02-22)
-------------------

* chore(release): add package-level `release:check` script and wire `release` to shared publish script.
* chore(scripts): replace `rm -rf` cleanup scripts with `rimraf`.
* (Changes generated/assisted by Codex (profile: chatgpt-5.3-codex/medium).)

v0.0.3 (2026-02-22)
-------------------

* chore(changes): add package CHANGES file in required format.
* (Changes generated/assisted by Codex (profile: chatgpt-5.3-codex/medium).)
